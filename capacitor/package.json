{
  "name": "acme-capacitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ../spa && npm run build && cd ../capacitor && npx cap sync",
    "ios": "npx cap open ios",
    "android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
